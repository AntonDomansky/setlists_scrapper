Python web-scrapping to parse band's setlists from setlist.fm.

Installation:
git clone https://github.com/AntonDomansky/setlists_scrapper.git
cd webscraper
pip install -r requirements.txt

Using:
python main.py